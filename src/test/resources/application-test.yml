spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/writeinone
    username: postgres
    password: secret

  datasource:
    url: jdbc:postgresql://localhost:5433/writeinone
    username: postgres
    password: secret

  flyway:
    enabled: true
